<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.lrj</groupId>
    <artifactId>drools-demo</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>drools-demo</name>
    <description>Drools learning scaffold: Hello World + Spring Boot order-discount example</description>

    <properties>
        <java.version>21</java.version>
        <drools.version>8.44.2.Final</drools.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.drools</groupId>
                <artifactId>drools-bom</artifactId>
                <version>${drools.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Drools 核心:
             - kie-api:           接口层 (KieServices/KieContainer/KieSession)
             - drools-core:       运行时 (RETE 算法实现)
             - drools-compiler:   DRL → Java bytecode 编译
             - drools-mvel:       支持 DRL 里的 mvel 表达式 (8.x 之后部分功能拆出来了)
             - drools-decisiontables: Excel/CSV 决策表 (留着方便第 3 步玩) -->
        <dependency>
            <groupId>org.kie</groupId>
            <artifactId>kie-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-compiler</artifactId>
        </dependency>
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-mvel</artifactId>
        </dependency>
        <!-- Drools 8.x 把 kmodule.xml 解析拆到独立模块，必须显式加，否则 ClasspathKieProject 启动报错 -->
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-xml-support</artifactId>
        </dependency>
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-decisiontables</artifactId>
        </dependency>

        <!-- Step 16: KieScanner + KJAR。kie-ci 带 Maven Aether 解析器, 让 KieContainer
             能按 ReleaseId 从 Maven 仓库 (含本地 ~/.m2) 解析 KJAR, 并让 KieScanner 轮询
             新版本。org.kie.scanner.KieMavenRepository 也在这个模块里, 用来把构建好的
             KJAR 装进本地仓库。注意: 这是个"重"依赖, 会拉进 maven-core / resolver 一票传递依赖。 -->
        <dependency>
            <groupId>org.kie</groupId>
            <artifactId>kie-ci</artifactId>
        </dependency>

        <!-- Step 17: DMN (Decision Model and Notation)。kie-dmn-core 是 DMN 运行时,
             传递带 kie-dmn-backend (解析 .dmn XML) + kie-dmn-feel (FEEL 表达式引擎)。
             加了之后 KieBuilder 会把 classpath 上的 .dmn 编译成 DMNModel,
             经 DMNRuntime (从 KieContainer / KieBase 取) 求值。跟 DRL 是两套独立体系。 -->
        <dependency>
            <groupId>org.kie</groupId>
            <artifactId>kie-dmn-core</artifactId>
            <!-- drools-bom 不管 kie-dmn-* 的版本, 显式锁到跟 Drools 同版本号 (KIE 同步发布) -->
            <version>${drools.version}</version>
        </dependency>

        <!-- Step 10: KieSession 持久化。
             - drools-serialization-protobuf: Drools 8 的 Marshaller 实现拆到独立模块,
               不显式引入 MarshallerFactory.newMarshaller(kb) 会报 "no Marshaller available"
             - spring-boot-starter-data-jpa: JPA / Hibernate
             - mysql-connector-j: 默认 (mysql profile) 用的数据库驱动
             - h2:              备用 (h2 profile), clone 仓库没装 MySQL 也能 file-based 跑起来
             两个驱动都留, 靠 spring.profiles.active 切换连哪个; 版本由 spring-boot BOM 管。 -->
        <dependency>
            <groupId>org.drools</groupId>
            <artifactId>drools-serialization-protobuf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Step 15: 规则可观测性指标。
             - actuator:                     暴露 /actuator/metrics、/actuator/prometheus
             - micrometer-registry-prometheus: 让 Micrometer 指标按 Prometheus 文本格式吐出来
             版本都由 spring-boot-starter-parent 的 BOM 管, 不写 version。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- P1-6 (活动引擎通用化): DRL→KieBase 有界缓存。通用化后 DRL 随 tenant/schema/活动/档位膨胀,
             原无界 ConcurrentHashMap = 更快 OOM,换 Caffeine 有界 LRU + 命中率统计。版本由 spring-boot BOM 管。 -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- P0-3 (Track B 多租户): 决策 API 接 Casdoor —— OIDC resource-server 本地 JWKS 验签,
             tenant 从 JWT 的 aud 解析(命脉实测 owner=admin 非组织; 不信 header/query)。默认关时
             提供一条 permitAll 安全链,不锁死 Step1~18 与静态页;开启时只护 /activity-marketing/**。
             版本由 spring-boot BOM 管。 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- 前后端分离：Vue3 前端工程在 frontend/。默认关闭(不进 ./mvnw test，保证后端迭代速度与零侵入)；
             用 -Pfrontend 触发 npm install + build 并把 dist/ 拷进 static/ui/(SPA 挂 /ui/，与旧 index.html 并存)。
             frontend-maven-plugin 本地下载 node/npm(不依赖机器全局 node)，构建可复现。 -->
        <profile>
            <id>frontend</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>1.15.1</version>
                        <configuration>
                            <workingDirectory>frontend</workingDirectory>
                            <nodeVersion>v22.12.0</nodeVersion>
                        </configuration>
                        <executions>
                            <execution>
                                <id>install-node-and-npm</id>
                                <goals><goal>install-node-and-npm</goal></goals>
                                <phase>generate-resources</phase>
                            </execution>
                            <execution>
                                <id>npm-install</id>
                                <goals><goal>npm</goal></goals>
                                <phase>generate-resources</phase>
                                <configuration><arguments>install</arguments></configuration>
                            </execution>
                            <execution>
                                <id>npm-build</id>
                                <goals><goal>npm</goal></goals>
                                <phase>generate-resources</phase>
                                <configuration><arguments>run build</arguments></configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>copy-frontend-dist</id>
                                <goals><goal>copy-resources</goal></goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <outputDirectory>${project.build.outputDirectory}/static/ui</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>frontend/dist</directory>
                                            <filtering>false</filtering>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
